# Quick Start

## Add to project

Add the following to your pubspec.yaml:

```
dependencies:
  chance_dart: ^[version]

dev_dependencies:
  chance_generator: ^[version]
  build_runner: ^[version]
```
