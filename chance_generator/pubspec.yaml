name: chance_generator
description: Extension for Chance Dart for generating random data
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  build: ^2.0.0
  source_gen: ^1.0.0
  chance_dart: 
    path: ../chance_dart
  analyzer: ">=4.6.0 <6.0.0"
  source_helper: ^1.1.0

dev_dependencies:
  test: ^1.17.11
  build_test: any
  build_runner: any
  pedantic: ^1.11.1
